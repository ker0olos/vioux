[build-system]
requires = ["maturin>=0.13,<0.14"]
build-backend = "maturin"

[project]
name = "vioux"
requires-python = ">=3.7"

[tool.isort]
profile = "black"
length_sort = true

dependencies = ["cffi"]